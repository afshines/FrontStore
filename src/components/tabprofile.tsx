import { DashboardMenu } from "@/sys/dashboard"

export default function TabProfile({ value }: any) {
    return (
        <div dir="rtl">
            {/* <Tabs
                value={value}

                scrollButtons="auto"
                aria-label="scrollable auto tabs example"
                textColor="secondary"
                indicatorColor="secondary"
              
                role="navigation"
            >
                {
                    DashboardMenu.map((item: any) => {
                        return (
                            <Tab href={item.url} label={item.name}>

                            </Tab>
                        )
                    })
                }



            </Tabs> */}
        </div>
    )
}